services:
  punk: &punk
    build:
#      context: ../../..
#      dockerfile: platforms/aarch64-linux-gnu/Dockerfile
      target: dev
    platform: linux/arm64
    volumes:
      - ../..:/punk
      - ccache:/ccache
      - opt_punk:/opt/punk
    env_file: '.env'


  punk-builder:
    <<: *punk
    build:
      target: builder
    entrypoint: ['just', 'all']
    restart: no

volumes:
  ccache: ~
  opt_punk: ~
