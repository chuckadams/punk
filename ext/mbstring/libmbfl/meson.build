# libmbfl's config header, which is just the PHP one.  It lives in this
# subdirectory because that is where libmbfl's sources look for it.
libmbfl_config_h = configure_file(
    input : 'config.h.in',
    output : 'config.h',
    copy : true,
)
